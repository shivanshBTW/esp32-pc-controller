# Copy to /config/secrets.yaml (merge if that file already exists).
# Same password as WakeType Settings / this repo's secrets.env.
# Keep the word Bearer and the space.

waketype_bearer: "Bearer YOUR_DEVICE_KEY"
